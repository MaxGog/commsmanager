# Example environment variables for Docker Compose
# Copy this to .env and set secure values before running

# Strong password for SQL Server 'sa' user (example only)
DB_PASSWORD=ChangeMeReplaceMe!

# API base URL used by the web client when running outside Docker
API_URL=http://localhost:5000
